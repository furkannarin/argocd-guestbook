apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: blue-green
  namespace: apps
spec:
  project: default
  source:
    repoURL: https://github.com/furkannarin/argocd-guestbook.git
    targetRevision: HEAD
    path: app-of-apps/blue-green
  destination:
    server: https://kubernetes.default.svc
    namespace: apps

  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
    - CreateNamespace=true
    - ApplyOutOfSyncOnly=true