apiVersion: batch/v1
kind: Job
metadata:
  name: wave-test-job
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  template:
    spec:
      containers:
      - name: reader
        image: busybox
        command: ["sh", "-c", "echo Job waits for wave 1"]
      restartPolicy: Never