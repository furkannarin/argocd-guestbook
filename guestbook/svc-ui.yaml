apiVersion: v1
kind: Service
metadata:
  name: svc
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  type: LoadBalancer
  ports:
  - port: 332
    targetPort: 80
  selector:
    app: guestbook-ui
